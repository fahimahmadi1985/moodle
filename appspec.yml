version: '0.0'
os: ubuntu
folders:
  - source: /moodle
    destination: /var/www/html/

hooks:
  BeforeInstall:
    - location: deployment-scripts/install-dependencies
      runas: root
    - location: scripts/start-server
      runas: root
